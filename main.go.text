package main

import (
	"nextzy-spinner-backend/internal/database"

	"github.com/gin-gonic/gin"
)

func main() {
	db := database.Connect()
	defer database.Disconnect()

	r := gin.Default()

	r.GET("/health", func(c *gin.Context) {
		c.JSON(200, gin.H{"db": db != nil})
	})

	r.Run(":8080")
}
